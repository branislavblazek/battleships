#ifndef SHIP_H
#define SHIP_H


typedef struct {
    int size;
    int startR;
    int startS;
    int isVertical; 
    int isSunk;
} Ship;

// Funkcie na prácu s loďami
//void placeShip(Grid* grid, Ship* ship);


#endif // SHIP_H
